# Installation

You can install `nhsuk-react-components` using a package manager of your choice.

```bash filename="npm"
npm install --save nhsuk-react-components
```

```bash filename="yarn"
yarn add nhsuk-react-components
```

```bash filename="pnpm"
pnpm add nhsuk-react-components
```
